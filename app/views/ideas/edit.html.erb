<div class="mx-4">

<br>
<%= form_for @idea do |f| %>
  <% if @idea.errors.present? %>
    <p>
      <%= @idea.errors.full_messages.join(", ") %>
    </p>
  <% end %>

  <%# 
    Although these different tags do not look like
    the tag methods used with `form_tag`, they take (mostly) the
    same arguments.
  %>
<div class="d-flex justify-content-center"><h1>Edit Your Idea</h1></div>
  <div>
    <%= f.label :title %> <br />
    <%= f.text_field :title, style: "width: 75%"%>
  </div>

  <div>
    <%= f.label :body %> <br />
    <%= f.text_area :body, style: "width: 75%", rows: 5 %>
  </div>

  <%= f.submit %>
<% end %>
</div>